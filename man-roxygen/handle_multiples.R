#' @param handle_multiples Character. How to handle multiple matches. One of
#' "list" or "first", or "error".
#' If "list", return a list-column with all matches.
#' If "first", return only the first match.
