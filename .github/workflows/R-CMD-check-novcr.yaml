# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # uncomment when https://github.com/ropensci/vcr/issues/549 is resolved
  # schedule:
  #   - cron: "30 2 * * 0"

name: R-cmd-check (no VCR)

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ubuntu-latest
    name: R-cmd-check (no VCR)

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
<<<<<<< HEAD
      MEETUP_TOKEN: ${{ secrets.MEETUP_TOKEN }}
      MEETUP_TOKEN_FILE: ${{ secrets.MEETUP_TOKEN_FILE }}
=======
      "meetupr:token": ${{ secrets.meetupr_token }}
      "meetupr:token_file": ${{ secrets.meetupr_token_file }}
>>>>>>> s7
      VCR_TURN_OFF: true

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "release"
          http-user-agent: "release"
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::rcmdcheck
            any::knitr
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'
